version: '1.0'

steps:
    unit-tests:
        image: verchol/node-tools:all
        fail-fast: false
        working-directory : ${{initial-clone}}
        commands:
            - echo "which git"
            - which git
            - echo "which go"
            - which go
