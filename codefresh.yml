version: '1.0'

steps:
    build-fanlin:
        image: alpine:latest
        description: "build fanlin"
        fail-fast: false
        working-directory : ${{initial-clone}}
        commands:
            - sh scripts/build.sh
    build-image:
        type: "build"
        fail-fast: false
        dockerfile: "Dockerfile"
        image-name: "ieee0824/fanlin"
        tag: "latest"
